logo should be changed to sticky rather than fixed after animation ends -> and have its end position as the default 
stuff in its css | on animation End -> delete the keyframe animation and put transition on the last state that it was and width to what it should be


images in preview when u click they open bigger but only load right there and then